name: Linux Static Build
on: [push, workflow_dispatch]

jobs:
  build-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-unknown-linux-musl

      - name: Install Build Tools
        run: |
          sudo apt-get update
          sudo apt-get install -y musl-tools cmake build-essential

      - name: Build Static Binary
        env:
          CC: musl-gcc
          CXX: musl-gcc
          # Force le mode statique pour Rust
          RUSTFLAGS: "-C target-feature=+crt-static"
          # Désactive l'assembleur complexe qui fait échouer BoringSSL en musl
          BORING_BSSL_NO_ASM: 1
        run: cargo build --release --target x86_64-unknown-linux-musl

      - name: Upload Linux Binary
        uses: actions/upload-artifact@v4
        with:
          name: ygege-linux-static
          path: target/x86_64-unknown-linux-musl/release/ygege
